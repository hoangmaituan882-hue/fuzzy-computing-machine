---
title: 候补名单
---

公开候补表单会验证邮箱和来源信息，并写入 PostgreSQL `waitlist` 表。管理员可分页查看并导出 CSV。

配置 `RESEND_API_KEY` 与 `RESEND_AUDIENCE_ID` 后，新邮箱会同步到 Resend audience；未配置时只落库，不影响报名成功。

Turnstile 和请求限流用于降低批量提交风险。新增来源参数时，应继续通过现有 source parser 和 CSV 转义函数，不能直接拼接不可信字符串。
