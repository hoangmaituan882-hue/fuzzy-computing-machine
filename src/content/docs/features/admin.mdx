---
title: 管理后台
---

在 `.env` 或生产环境中设置：

```dotenv
ADMIN_EMAILS=admin@example.com,second@example.com
```

这些邮箱登录后会同步为管理员。`/{locale?}/admin` 父路由统一执行服务端管理员校验，普通用户不会只靠前端隐藏来隔离。

当前后台包括：

- 用户统计、搜索、角色、封禁和模拟登录
- 三个群身份卡的标题、说明和图片
- 候补名单与 CSV
- 赞助记录
- 用户反馈治理

竞猜活动本身仍缺少开场、关场、选中提名和完成放映的管理流程，这是下一阶段的核心后台工作。
