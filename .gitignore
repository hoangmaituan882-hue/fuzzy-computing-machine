node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

.dev.vars*
!.env.example
# 旧 Cloudflare 密钥文件仍保持忽略，迁移完成后可删除相关配置。
.prod.vars
.staging.vars

# 旧 Wrangler 本地配置可能包含真实资源 ID，绝不入库。
wrangler.jsonc

# Local-only verification screenshots (not distributed with the template)
docs/superpowers/archive/screenshots/

# Playwright e2e artifacts
e2e/screenshots/
test-results/
playwright-report/
.playwright/

.source/

# Claude Code session config (local)
.claude/
