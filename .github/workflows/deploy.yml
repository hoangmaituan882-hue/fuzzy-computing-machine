name: Deployment notice

on:
  workflow_dispatch:

jobs:
  docker-deployment:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic Cloudflare deployment is disabled."
          echo "Deploy the Node.js + PostgreSQL stack with docker-compose.yml; see DEPLOY_1PANEL.md."
